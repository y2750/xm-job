<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.WithdrawalRecordMapper">
    <insert id="insert" parameterType="com.example.entity.WithdrawalRecord" useGeneratedKeys="true" keyProperty="id">
        insert into withdrawal_record (user_id, user_type, amount, fee, actual_amount, status, bank_account, bank_name, remark, created_at, updated_at)
        values (#{userId}, #{userType}, #{amount}, #{fee}, #{actualAmount}, #{status}, #{bankAccount}, #{bankName}, #{remark}, #{createdAt}, #{updatedAt})
    </insert>
    
    <update id="updateById" parameterType="com.example.entity.WithdrawalRecord">
        update withdrawal_record
        <set>
            <if test="status != null">status = #{status},</if>
            <if test="updatedAt != null">updated_at = #{updatedAt}</if>
        </set>
        where id = #{id}
    </update>
    
    <select id="selectById" resultType="com.example.entity.WithdrawalRecord">
        select * from withdrawal_record where id = #{id}
    </select>
    
    <select id="selectByUserIdAndType" resultType="com.example.entity.WithdrawalRecord">
        select * from withdrawal_record
        where user_id = #{userId} and user_type = #{userType}
        order by created_at desc
    </select>
</mapper>

